# Branching smoke test

Throwaway PR to verify the Supabase branching setup end-to-end:
GitHub App creates a preview branch, migrations + seed apply, and the
Vercel preview deployment receives the branch's credentials.

This file is deleted when the test PR closes.
